import valuestest, accumulatortest, expressionreadtest, expressionstest, jobstest, lineparsingtest,
    qualifierstest, readertest, serializetest, testutil, valuestest, formatterstest

when isMainModule:
    echo "Done running tests"